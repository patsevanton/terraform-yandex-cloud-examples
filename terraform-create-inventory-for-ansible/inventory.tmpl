[${application1_name}]
${application1_name} ansible_host=${application1_external_ip_address} # or ansible_host=${application1_internal_ip_address}

[patroni]
%{ for index, name in patroni_name ~}
${name} ansible_host=${patroni_external_ip_address[index]} # ${patroni_internal_ip_address[index]}
%{ endfor ~}